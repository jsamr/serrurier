{
  "scripts": {
    "test":"meteor test-packages ./ --once --driver-package dispatch:mocha-phantomjs",
    "test:watch":"TEST_WATCH=1 meteor test-packages ./ --once --driver-package dispatch:mocha-phantomjs"
  }
}